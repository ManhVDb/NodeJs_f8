
<div class="mt-4" >
    <h3>Cập Nhật Khóa Học</h3>

        <form method="POST" action="/courses/{{show._id}}?_method=PUT" class="madttb">
    <div class="form-group">
        <label for="name">Tên khóa học</label>
        <input type="text" class="form-control"  value="{{show.name}}" id="name" name="name" placeholder="Nhập tên khóa học">
    </div>

    <div class="form-group">
        <label for="desc ription">Mô Tả</label>
        <input type="text" class="form-control" value="{{show.description}}" id="description" name="description" placeholder="Nhập nội dung khóa học">
    </div>

    <div class="form-group">
        <label for="videoId">Video ID</label>
        <input type="text" class="form-control" value="{{show.videoId}}" id="videoId" name="videoId" placeholder="Nhập id Video">
    </div>


    <button type="submit" class="btn btn-primary">Lưu</button>
    </form>
</div>